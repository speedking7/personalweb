# AI Agent 实战系列 · 内容规划 v0.1

> 状态：待站主审阅。发布前逐篇过脱敏检查。
> 载体：公开博客新轨，标题带序号（《AI Agent 实战 03 · …》），零前端改动。
> **方法平台无关**；knodo 只作演示载体，换 fastgpt 等平台方法同样成立。

---

## 零、当前进度（2026-09-08 更新，接手先读这一节）

### 已发布

| 篇 | 标题 | 博客 | 公众号 |
|---|---|---|---|
| 00 | 这个系列讲什么 | ✅ | ✅ 已回填 `wechat:` |
| 01 | 四步搭出一个问答 Agent | ✅ | ✅ 已回填 `wechat:` |
| 02 | 三个低风险特征，四类升高因素 | ✅ | ✅ 已回填 `wechat:` |
| 03 | L1 到 L4：按动作定级 | ✅ | ✅ 已回填 `wechat:` |
| 04 | L4 改造：批清单，不批结论 | ✅ | ✅ 已回填 `wechat:` |
| 05 | 登记表：二十个字段谁填哪一格 | ✅ | ✅ 已回填 `wechat:` |
| 06 | 角色卡：先写它不干什么 | ✅ | **已进草稿箱，待站主发表** |

**05 的待办**：2026-09-10 已送草稿箱，正文三处内链全部改写。剩下人工四步同前。
**草稿 media_id**：`ANMvyPTNsZJYdcfdSl5doOrxzCOkqolF9Zj7wmcQlNVBR0wiKN3IIGynV6SEXrc3`

**「先发五篇」到此齐了。**第七节那个目标（发完 01–05，学员能自己判断该做什么、找谁）已达成。
其余十七篇按原计划慢慢补，下一篇是 06（角色、目标与非目标清单），进第三部分「Agent 合同」。

**04 全流程已走完**（09-09 送稿、09-10 发表并回填）。media_id：
`ANMvyPTNsZJYdcfdSl5doDCU9Iuy--hdOMzj9ySDXbohnGHoAhDV1NMdrXyFTm8l`

**03 全流程已走完**（2026-09-08 送稿、09-09 发表并回填）。它那次的 media_id：
`ANMvyPTNsZJYdcfdSl5doB1J4xRozaBkUuJSt0j21XbQI-ho-MTHvAPosY7k-ejx`。
02 那次的只活在当次输出里、已经找不回来，从 03 起逐篇记进本文件。

**每次回填都要知道的一条**：那条链接机器核不了——微信长链不带 `chksm` 直取弹「环境异常」，
浏览器桥也撞在 `wappoc_appmsgcaptcha` 验证墙上，而读已发布链接的 API 对个人主体早已回收
（见 `guides/WECHAT_SYNC.md`）。能做的只有确认它与已存的都不同，最终以人眼为准。

**发布不是构建，是推送（2026-09-08 的教训）**：文章落进 `posts/` 是本地动作，
`npm run build` 也是本地动作，**只有 `git push` 之后 GitHub Pages 才拿得到**。
那次连推了五个提交才发现线上一直停在旧版本，而本地一切正常、闸门全绿。
现在的规矩是：**文章类改动构建完直接推**，工具与底稿类改动可以攒着。
验证方式是比对线上 `index.html` 里的 bundle 文件名与本地 `docs/assets/` 是否一致。

### 下一篇是 05（项目输入登记表：二十个字段逐项填写）

04 已发布，第二部分（定级与立项）只剩 05 这一篇。05 是前三篇产出物的收口：
自评表、定级表、改造对照表在那二十个字段里都有位置，写的时候逐一对上，别另起炉灶。

<!-- 04 发布前的记录 -->

草稿在 `drafts/agent-04-approve-the-list.md`，3787 字，闸门 7/8（差封面）。
**送稿有个顺序硬约束**：04 正文三处链接指向 03，而公众号互指只能指向已发过的文章，
所以 04 送草稿箱必须排在 03 发表并回填 `wechat:` 之后，否则 `build.mjs` 会中断。

**那件待验的事 2026-09-08 已办**：站主在演示空间建任务、执行者设成 Bot、点「立即执行」，
它执行完**直接停在「已完成」**，中间没有审查环节，界面无提示。🟢 已记进
`knodo-platform-facts.md` 第七节。

这比原来那条文档断言值钱：不是「可能会静默消失」，是**在演示空间里当场消失了一次**，
而消失的正好是 04 篇通篇在讲的那个批准点。第六节因此从「一个要去确认的地方」
改成了实测依据，并顺势成了本篇的一个落点。

**但归因未分离，文章口径只到行为为止。**观察到的是「它没停」，
「原因是状态配置里缺『人工审查』那一档」这半句仍未验证——要分离得先看状态配置里有没有
那一档、有则加上再跑对照。这跟 `02-model-comparison.md` 第五节那个未分离归因是同一类欠账，
一并记在这里，别在后面篇目里当成已证事实用。

### 再下一篇是 05（项目输入登记表：二十个字段逐项填写）

动笔前必读：本文件第四节写作规范、第六节 04 与 09 的条目，
以及**第 03 篇长出来的那条判据**（下面第一小节）——04 整篇是它的一次应用。

### 05 篇长出来的两条，写 06/07 前必读

**其一，「禁止用途」这一格跨 06 与 07 两篇，别再混。**05 初稿把它整格路由给 06，
被评审拦下——序章公开目录里 06 是「角色、目标与非目标清单」、07 是「动作分层与禁止动作清单」，
而且**序章的公众号版已发、改不了**。现在 05 的口径是：「它不替谁干什么活」归 06 的非目标清单，
「不许做哪些动作」（不得替人定级、不得直接催办、不得在窗口外发起上线）归 07 的禁止动作清单。
06、07 动笔时照这个分。

**其一之补正（2026-09-11，写 06 时撞出来的）**：上面那条分法留了一道缝——
「不替提单人判风险等级」这类条目**同时具备职责形态与动作形态**，两边都套得上，
06 初稿据此把它整条划进非目标，跟已发布的 05 篇正面冲突。**规矩改成：这类条目两边各写一条，
措辞分开。**职责形态「不承担『这次算几级风险』这个判断」归 06，动作形态「不得替提单人给出
风险等级结论」归 07。职责不写，07 那条就没有出处；动作不写，职责就落不了地。写 07 时照这个来。

**判据也一并修正**：06 初稿用的是「工程搭档能不能直接翻译成可执行约束」，被评审用本篇自己的
例子证伪了——「知识库里只放这三份」结构上挡得住，它仍然是非目标。**能不能挡住，和它属于哪一层，
是两件事。**现在的判据是：说的是一段持续的职责归属（归 06），还是一次可以被拦下来的动作（归 07）。

**其二，登记表是后面每一篇的收口容器。**06–10 那九个控制项、11 的知识库、13 的接口、
16 的评测集、18 的证据包、19/20 的监控与退出，各自的产出物在二十个字段里都有对应格子。
写那些篇时**明说填进哪一格**，别让读者自己找——这是 05 给后续篇目留的接口。

### 两笔待办，记着别丢

**其一，「成功算什么」在二十个字段里没有专格。**06 的角色卡有这一行，登记表接不住，
现在暂占「运行监控和复盘机制」。而第 18 篇上线证据包第一条就是「业务目标、价值指标和成功标准」
（素材源第 8 节），写到 18 时决定要不要给登记表回头补一格。这是登记表本身的缺口，不是某一篇的错。

**其二，「同一空间多个助手共享知识库」仍是 🟡 未实测**（`01-knodo-operations.md` 第三步）。
**06 篇已经把它整条拿掉**——那一段改用「建一个新助手只要名称加一段提示词」（01 篇已实测）作地基，
论证照样成立。**验证欠账不进文章，这条 2026-09-11 已写进 `BLOG_PLAYBOOK.md` 第三轨那一节。**
但这条事实本身仍是第 21 篇「拆多个 agent 拆的是提示词与职责，不是知识」的地基，
哪天在界面上建个第二助手看一眼就能升 🟢。

### 03 篇长出来的、后面篇目要接着用的三样

**其一，定级用两把尺子，不是一把。**三道分界线量的是「它碰什么」；
而只读、只生成的动作也可能是高风险，因为**它的话被下游当成了什么**是另一把尺子。
03 篇的落点写成了「过了线一定升档，没过线不等于不升档」。
这一条是 04 篇（换动作降级）与 09 篇（批准对象）的共同地基，**别在那两篇里另起一套说法**。

**其二，定级的对象是动作不是 Agent，整体取最高。**演示知识库那张《变更风险分级表》
末尾「同一张单命中多条的，按最高等级确定」原样借用，读者手上就有。
05 篇登记表里那格「风险等级」收的就是 03 的产出表（动作清单 + 逐条等级 + 依据 + 谁能改模型）。

**其三，L2→L3 那条线的判据修正：「给别人发一条通知」才算过线。**
收件人是它自己的使用者不算——否则 L1 那两行（草稿也是交给本人）当场塌掉。
**这跟 02 篇因素表里「自动通知提单人」那个例子有张力**：那里通知的是流程里的另一方。
07、09 篇写到通知与审批点时，措辞要跟 03 收敛，别自相矛盾。

### 03 这一轮的其他账

**篇幅**：03 是 4166 字 / readTime 12，比 01（3202）02（3026）高三成。
方法篇天然更长（12 张表），但 preflight 回填实战轨目标值时要把方法篇与操作篇分开看，
别拿一个区间套两种篇目。

**平台事实补了一条**：查「它现在被允许做什么」要看**四处**不是三处——
空间设置「AI 配置」下的「插件与技能」跟 Bot 编辑弹框里跟着助手走的「个人技能」
**两处都能绑、作用域不同**（🟢 亲验，见 `01-knodo-operations.md` 第三步）。第 12 篇要分清。

**一条查证了才能写的**：`knodo-platform-facts.md` 第六节记着平台组织统计里有个
「AI 落地评级（L1–L4，5 维雷达）」——**跟本系列的风险分级同名不同物，读者会当场撞上**。
但那条是通读代码推出来的、没有实测标记，**按本文件第四节那张自查表不该以事实形态进文章**，
03 篇因此没写。哪天去分析页点一眼确认了，补进 03 或 19 篇都行。

**23 篇的素材被 04 提前用掉两条半**：不信 agent 自我报告、覆盖率平台自己算、
diff 平台自己从版本库提取——第 23 篇原本要拿这三条当收尾案例，写到那里得另找。

**发布侧新增一条经验**（已记进 `BLOG_PLAYBOOK.md` 第五节与 `scripts/preflight/CLAUDE.md`）：
封面内缩后补边要用**镜像平铺**而非纵向拉伸，拉伸会把干净带自身的横向渐变放大到超容差、
逼得退回纯色丢掉纸纹，而平铺的纸纹标准差与拉伸完全相同。`fit.py` 已改，平铺排在拉伸前。

### 演示环境的现状

knodo 上有一个在跑的演示空间 `研发流程问答（演示）`，里面有 Bot `流程助手`、
三份虚构流程制度（`01-demo-kb/`）、以及五道验收题的会话。均为 `PRIVATE`。
凭据在 `~/projects/knodo/.env`（`KNODO_PAT` / `KNODO_API`），
**注意 `/files/tree` 用参数 `path`、`/files/content` 用 `filePath`，同一套 API 里不统一**。

### 这一轮踩出来的、影响后续每一篇的东西

**发布侧三个雷**（全部记在 `BLOG_PLAYBOOK.md` 第六节）：
markdown 表格默认不渲染（已装 `remark-gfm` 并补 `.prose table` 样式）、
有序列表在公众号里散架（源头别用，改二列表格）、
实战轨标题只剩 15 字预算（前缀「AI Agent 实战 NN · 」占 17 字，公众号上限 32）。
03 篇原拟《风险分级 L1–L4：判定方法与依据》18 字超预算，
改成《L1 到 L4：按动作定级》13 字——**取舍规矩照旧：保结构索引，舍说明性措辞**。

**`prose.py` 闸门增到八条**，其中两条是条件触发。

**加粗被中文标点吃掉这条犯得极频繁**（02 篇一篇修了七处，03 篇初稿三处）。
写完用这段自动扫，判据照抄闸门那套 CommonMark 按序配对，别用正则找 `**...**`：

```python
# 把闭合 ** 前的标点挪到星号外面，直到扫不出为止
marks=[m.start() for m in re.finditer(r'\*\*', line)]
for k in range(1,len(marks),2):   # 奇数下标是闭合位
    i=marks[k]; prev=line[i-1]; nxt=line[i+2]
    if prev is punct and nxt is not space and nxt is not punct:
        line = line[:i-1] + '**' + line[i-1] + line[i+2:]
```

---

## 一、这个系列教什么

**不是教怎么写提示词，是教怎么把一件业务上的活，安全地交出去一部分。**

学员学完能主导一个 agent 项目：知道要干哪些活、哪些能交给 AI、哪些是自己作为
产品经理必须拍板的、以及做出来之后怎么判断它行不行。

**不教的**：模型原理、编程、平台运维。需要写代码的地方明确说「这里要找工程搭档」，
并教到「能跟工程师对话」为止。

## 二、读者与前提

业务专家，梳理需求是强项，**没有开发经验，没搭过 agent**。
手上有真实需求想自己动手。

因此全系列两条纪律：术语先讲动作再报名字；每个抽象判断后面必须跟一个
他们业务里真实发生过的场景。

## 三、主轴：风险分级

贯穿全系列的那根轴不是技术难度，是**风险等级**。
一个 agent 能自动到什么程度，不看它多聪明，看它碰什么。

| 等级 | 它在干什么 | 能自动到什么程度 |
|---|---|---|
| L1 辅助生成 | 写草稿、做总结、解释东西 | 人看过再用 |
| L2 受控问答 | 查制度、查资料、答疑 | 来源可追溯，按权限检索 |
| L3 受控执行 | 建任务、填单据、发起流程、调内部接口 | 白名单 + 最小权限 + 关键动作人工批 |
| L4 高影响执行 | 碰钱、碰客户承诺、碰合规判断、碰生产 | 原则上不允许无人值守 |

依据 ISO/IEC 42001 的管理思路整理。**这张表是全系列引用最多的一张。**

## 四、写作规范（与现有博客不同轨，动笔前先读）

现有七篇是专栏，靠钩子拉着读者线性往下走。**这个系列是操作手册，读者是查阅式使用的**——
搭到一半卡住，回来扫目录找那一篇。两种材料的使用方式相反，写法必须跟着换。

**标题是索引，不是钩子。**说清这一篇讲什么、能得到什么，结论放进标题里，不藏。
不用设问句、不用悬念、不拟人化。判据：学员两周后回来查资料，看标题能不能想起里面讲了什么。

**段落节奏是「这一步做什么 → 怎么做 → 做完应该看到什么 → 常见错误」**，
不是「设问 → 短答案 → 展开」。后者是专栏的引擎，用在操作手册上会让读者找不到重点。

**术语直接用，第一次出现给一句定义就够。**读者是业务专家不是零基础大众，
不需要生活化比喻做台阶。比喻可以有，但只能用来解释，不能承重。

**表格优先于散文。**分级、清单、矩阵、字段说明，一律用表格。合规文档本身就是表格为主，
照抄形态。表格在博客与公众号都原生渲染，读者还能直接复制去用。

**平台无关的方法与平台具体操作必须分层写，并显式标注。**
每篇先讲方法——换个 Agent 平台照样成立的那部分；再给 knodo 上的具体点法作为一次实例。
不分层的话，换平台整篇作废，读者也分不清哪些是原理、哪些是这个平台的特性。

**每篇必须有产出物。**学完手上要多一个东西：一份填好的表、一个跑起来的 Agent、
一套评测集。没有产出物的篇目说明还没想清楚它存在的理由。

**先让读者碰到问题，再给设计（2026-09-06 站主定，全系列适用）。**
案例一开始要简单，让读者自己撞上问题——或者**主动设计一些会让他撞上的题**——
之后再讲在有限资源下怎么改善。**授之以鱼不如授之以渔。**

**这条我们其实已经在用，只是此前没意识到它是法则**：第 01 篇那五道验收题里，
第 3 题设计成要跨两份文件、第 4 题设计成文件里根本没写，那不是在考 Agent，
是在让读者亲眼看见它在哪儿不行。

**违反它的典型形态**：把一套成熟方案整套端给读者。读者读完知道有这么个东西，
但他没碰过那个问题，学不会为什么要这么做，更学不会换个前提怎么另想一套。
第 02 篇初稿在「知识组织」那一节就是这么写的，改稿时整段换成了
「你会在什么时候碰到 + 那时候症状长什么样」，方案交回第 11 篇。

**落地判据**：写一节之前先问——**读者到这里，有没有已经吃过对应的亏？**
没有的话，这一节应该是「让他吃亏」或「告诉他亏会长什么样」，而不是给药方。

**同一个错在 2026-09-06 一天里犯了三次，形态一模一样，写每篇前都要自查：**

| # | 犯法 | 真相 |
|---|---|---|
| 1 | 「常见错误」九条里六条是从「听起来合理」推的 | 只有三条有实测 |
| 2 | 把一整套成熟的知识组织方案端给读者 | 那是一类调优手段，不是标准答案 |
| 3 | 预设「一致性要靠知识治理 Agent 维持」 | 那也只是手段之一，要看规模与频率 |

**模式是同一个：手上有一个答案，就忍不住把它当成「那个」答案。**
自查一句话：**我要写的这条，是唯一解，还是我恰好知道的那一解？**
是后者，就得写成「一类做法」并给出它的适用前提与代价，
或者干脆只讲问题、把解法留给读者。

---

## 五、样例先行（必须在写文章之前）

**先按教学路径真搭一遍，再写文章。**不是为了配图，配图只是副产品。

理由是现成的：knodo 摸索项目里最值钱的那些结论——「已发布／已绑定／组件非空」三者
每一层都替代不了下一层、超时的更新请求其实持锁挂起导致后续三小时全被拒、
组件列表在重传期间会瞬时归零——**没有一条是读文档能读出来的**。
不真搭一遍，写出来的会是「应该是这样」，而不是「实际是这样」，
而这个系列的全部价值在后者。

做法：建一个中性命名的演示空间，按 01–05 的顺序真搭一个研发流程 Agent，
边搭边记。**搭到第几步对应第几篇，卡在哪儿就是那一篇该写的内容。**

### 图分三类，做法完全不同

| 类型 | 占比 | 做法 |
|---|---|---|
| 操作截图 | 绝大多数 | 真实界面，加箭头与框。不需要设计，只要清晰 |
| 结构图 | 少量 | 流程、层级、边界。**字号按正文容器宽度反推**——本站容器桌面端 574px，画布上排的字缩完会看不清；正文插图第一标准是看懂，不是好看 |
| 表格 | 很多 | **不要做成图片。**分级表、登记模板、权限矩阵一律用 markdown 表格 |

把第三类摘出去之后，真正要做的图少一半，剩下的都是非做不可的。

### 三个工程约束

**公众号素材配额。**正文图片要传微信永久素材库，而永久素材有数量上限。
一篇十几张截图、二十二篇就是两三百张。按内容 hash 去重只对重复图有效。
量要提前算，或接受某几篇公众号版不带图。

**页面重量。**十几张截图一篇，加载与流量都要考虑，截图必须压缩。

**截图脱敏。**平台界面会带空间名、组织名、用户名、头像。
**从源头解决**——专门建一个中性命名的演示空间，别指望每张图手工涂抹。

---

## 六、二十三篇

### 第一部分 · 先看见它（2 篇）

**01 · 创建第一个问答 Agent（全程无代码）**
全程点击，不写一行代码。建空间、传几份资料、写一段 System Prompt、跟它对话。
先让他们看见 agent 长什么样——没搭过的人，讲再多概念都是空的。
> 产出：一个真在跑的问答 agent
> 坑：知识库里**放什么、以及放了多少**决定它答得准不准，第 11 篇细讲。
> **「多少」这个变量是 2026-09 实操加的**：知识库小到能全量读进上下文时，
> 它根本不走检索，所以答得准——**那个准是规模给的不是能力给的**，
> 而学员在十份文件上验收通过、上线时是三百份，不会预期到这个拐点

**02 · Agent 的风险来源与分级依据**
以第 01 篇的产物为对象，说明它属于只读、只生成、结果可自检三种低风险特征，
因而无需额外控制。再列出四类使风险升高的因素：写入系统、影响外部方、
结果不可自检、动作不可逆。
> 产出：能说明「能否直接上线」由风险特征决定，而非技术难度
> 覆盖：低风险特征、四类风险升高因素
> **模型是第五个风险因子（2026-09-06 实操新增）**：knodo 上 Bot 与模型解耦——
> Bot 定「它是谁、按什么规矩办事」，模型定「用什么算」，而模型是**对话级可换的**。
> 实测同一道题、同一知识库、同一段提示词，qwen3.8-flash 三处引用两处有问题，
> glm-5.2 全对且多标出三个知识库空洞。**两版读起来同样自信规范，
> 你无法从答案本身判断可靠性。**这条比原设想的「出处会编」有力——
> 「会编」听着像能靠经验规避，这个不是。第 03 篇定级时它是**分级表里没有的那一格**

### 第二部分 · 定级与立项（3 篇）

**03 · 风险分级 L1–L4：判定方法与依据**
L1 到 L4 怎么判，判据是什么。**三个对照场景取自同一条流水线**（2026-09 改，见第七节）：
查流程制度答疑（L2）、帮人提单并追踪进度（L3）、自动修复并发布到生产（L4）。
同场景对照的好处是变量只剩一个——**它碰什么**——技术难度、领域、人员全都不变，
读者看得见分级到底由什么决定。
> 产出：给自己手上的需求定级，并写出定级依据
> 关键：定级不是走过场，它决定后面所有控制强度

**04 · L4 场景改造：把批准对象从结论换成清单**
解剖 L4 场景：自动修复一个支付回调的缺陷，并发布到生产。
按标准它同时命中资金链路、写入生产系统、动作不可逆、自动执行四条。
**但出路不是砍掉自动化，是把「人批准的对象」从结论换成清单。**
> 产出：把一个过不了门禁的方案改造成能过的
> 覆盖：定级复核、审批对象设计、执行一致性约束
> **2026-09 改**：原解剖对象是「对账后自动开票」。换成同一条流水线上的发布场景，
> 是为了跟 03 篇共用变量——三个对照场景只剩「它碰什么」一个变量在动。
> 「碰钱」这一维靠支付回调保住，没有丢。额外好处是它跟第 09 篇（审批粒度）
> 与第 21–23 篇（发布队列与批次）直接连上。**这一处是判断，不是你点的头，可回滚。**

**05 · 项目输入登记表：二十个字段逐项填写**
二十个字段的输入登记模板：业务目标、系统边界、风险等级、责任分工、
数据清单、工具权限、人工控制、测试要求、运行监控、退出机制。
逐字段讲怎么填，哪些自己能填、哪些必须去找 Owner。
> 产出：一份填好的登记表——这是项目本来就要交的东西，不是练习作业

### 第三部分 · Agent 合同（5 篇）

这五篇讲的是搭**任何**一个 agent 都要过的九个控制项。
差异只在工作内容、技能、输入输出；这九项对每个 agent 都一样。

**06 · 角色、目标与非目标清单**
一句话说清它替谁干什么活。重点在**非目标清单**——写不出「它不干什么」，
说明职责还没想清楚。
> 产出：角色卡的上半张

**07 · 动作分层与禁止动作清单**
把动作拆成读取 / 生成 / 建议 / 提交草稿 / 触发流程 / 写入系统 / 对外发送七层。
层级越往后风险越高。禁止清单要显式写，例如不得绕过审批、不得直接承诺客户。
> 产出：动作权限矩阵
> 关键：这一层没拆清楚，后面的审批点就没法放

**08 · 工具白名单与最小权限配置**
默认只读；要写权限必须按场景、对象、字段、操作拆开授权。
工具不止平台内置的那些，也包括对外部系统的调用——接口从哪来见第 13 篇。
「让它自由选择工具」是明确不允许的实现方式。
> 产出：工具清单 + 权限矩阵

**09 · 人工审批点：位置、粒度与一致性**
接第 04 篇。**批准的必须是一份具体可执行的清单，不是汇总结论；
批准之后执行的必须严格等于那份清单，不能重新生成。**
还要定粒度：一次确认授权五百次写入，那这一次点击就扛了五百次的责任。
> 产出：审批点设计 + 粒度规则
> 覆盖：批准对象的粒度、批准与执行的一致性校验

**10 · 可追溯日志与版本管理**
要记的字段：用户、输入、引用来源、模型版本、提示词版本、工具调用、输出、
审批人、执行结果。
> **两个平台侧的真缺口（2026-09 实操）**：会话消息 35 个字段里**没有任何出处类字段**，
> 出处是模型写在正文里的；`modelId` 字段**存在但实测全部为 `null`**。
> 也就是说「引用来源」与「模型版本」这两项，**平台目前一项都不满足**。提示词、工具、知识库、模型、评测集变更都要留版本。
> 产出：日志字段设计
> 判据：出了问题能不能还原「它当时为什么这么做」

### 第四部分 · 在平台上落地（4 篇）

**11 · 知识库：来源、版本、有效期与废止机制**
知识库不是文件堆。每份知识要有来源、版本、更新时间、失效时间、责任人。
同一问题多个口径要提示冲突；引用过期文档要显式标记。
> 产出：知识库目录
> 坑：把领域知识塞进全局指令，会固定占用上下文且丢失出处
> **知识组织即调优手段（2026-09-06 站主提供，本篇的主体内容）**：
> 在不接外部 RAG 服务、不上知识图谱的前提下，**只靠目录与索引结构**也能显著提升
> 命中效率与准确率。已在用的一套做法：按业务域分编号目录、每层放一份只做导航的索引文件、
> 每份知识写元信息，再给 Agent 一条**检索预算**——根索引 → 一个模块索引 →
> 最多两份主题文件，命中即停。**这就是渐进式披露的工程形态。**
> 另有一条防串味的约束：索引文件里明写「不许把索引本身当业务依据引用」。
>
> **必须同时讲代价，否则本篇会变成推销**：设计复杂度上升；每次维护知识要保证
> 目录、索引、元信息三者一致，**漏一处地图就和地形对不上，模型会被稳定引到错的地方**。
> **控制手段本身也有成本**——这条接第 05 篇的登记表。
>
> **一致性怎么维持，本篇不预设答案（2026-09-06 站主纠正）。**手段有好几种：
> 人工评审、脚本校验、发布前清单、专门技能、独立的治理 Agent……
> **用 Agent 治理也只是其中一种手段，要看知识规模与变更频率再定，不该提前指定。**
> 本篇要做的是把「不一致会付什么代价」讲清楚，让读者自己算这笔账。
>
> **写法上要说清这只是一类手段不是唯一解**，选哪种取决于知识规模、类型与预算。
>
> **规模拐点（2026-09 实操新增，本篇独有）**：小知识库它全量读、不检索，所以准；
> 大到读不动才开始走检索，而这个平台的检索只有关键词/文件级。
> **第 01 篇那个「答得准」的体验会在这里失效**，本篇要负责把拐点讲明白并给出判断方法。
> 另：「同一问题多个口径要提示冲突」实测它在小规模下**自发做到了**（提示词里没写）——
> 但一次观察、模型自发，**不可当作可依赖行为**，要变成可依赖得写进提示词并用评测集验（第 16 篇）

**12 · 无代码的边界：哪些环节必须写代码**
划清无代码能走到哪：建空间、传知识、写 System Prompt、建任务、配定时、
设审查泳道——全都点得出来。**卡点在打包、注册、验证这三处。**
教到「能跟工程师说清楚要什么」为止。
> 产出：判断自己的需求要不要找搭档

**13 · 外部系统接入：接口盘点、缺口识别与源系统协作**
Agent 要干活就得调别的系统，而源系统未必已经提供了可调用的 API 或 MCP。
三个动作：盘点现有能调什么（API / MCP / RPA / 定期导出文件）；识别缺口——
拿不到是权限问题还是压根没接口；协作——写接口需求说明、谈权限范围（只读还是可写、
限定哪些对象与字段）、约定 SLA 与故障责任人。
还要讲清接口做不出来时的退路：人工导出上传、RPA 抓屏各自的代价与适用边界。
> 产出：接口需求清单、权限申请材料、退路方案
> 覆盖：对应合规输入包的「工具权限」与 A.10 第三方关系（接口方责任与 SLA）
> 说明：这块工作量常超过搭 Agent 本身，且属于业务顾问的强项——
> 跨部门协调、把需求讲清楚、争排期

**14 · 门禁的执行位置：必须在 Agent 之外**
沙箱里的 agent 手上有 shell，能跑脚本——也因此能绕过你给它划的边界。
**它自己跑门禁再报告「过了」，跟直接说「我做完了」没有区别。**
门禁必须在 agent 之外执行，结果写回来。
> 产出：理解「哪些交给 AI、哪些必须确定性」这条线画在系统边界上
> 覆盖：执行位置、结果回写、越权绕过的实测形态

**15 · 异常路径：五种必须停下来的情况**
模型不确定、知识缺失、权限不足、工具失败、结果冲突——五种情况都必须停下来找人，
而不是猜一个继续往下走。
> 产出：异常路径清单 + 对应的测试用例

### 第五部分 · 测试与上线（3 篇）

**16 · 评测集设计：标准问答、负例与回归**
几个正向样例不算测试。要有标准问答集、要有负例、要有回归——
知识库更新之后，原来答对的问题不能变答错。
> 产出：一套评测集

**17 · 红队测试：四类攻击面**
四类：提示注入、越权访问、错误工具调用、敏感信息泄露。
**提示词管不住「不做什么」**——这一条要用实测案例讲透。
> 产出：红队测试报告

**18 · 上线证据包：十四项清单与五道门禁**
十四项证据清单。核心原则一句话：**没有证据就不算完成，演示可用不等于可上线。**
> 产出：完整证据包
> 配套：G0 立项 / G1 设计 / G2 测试 / G3 上线 / G4 复盘 五道门禁怎么过

### 第六部分 · 运行（2 篇）

**19 · 运行监控：平台指标与自建指标**
平台原生给的：用量趋势、调用排名、使用热力图。
**平台不给的：答得对不对、有没有过期、有没有胡编。**那部分要自己造。
监控错误率、人工接管率、用户反馈。
> 产出：监控看板设计 + 告警规则

**20 · 退出机制：暂停条件、降级与回滚**
触发暂停的五种情况、降级方案、回滚预案。以及更常见的那种慢性退化：
知识过期、口径漂移、人工接管率悄悄升高。
> 产出：退出机制 + 复盘节奏

### 第七部分 · 多个 Agent（3 篇）

**21 · 流程拆分：职责边界与降级判定**
以一条开发流水线为例：理解需求并拆任务、写测试、写代码、跑测试、评审、发布。
每个 agent 一份合同，九个控制项逐个填。
> 覆盖：职责切分方法；降级判定——发布与测试执行属确定性设施，
> 仅「失败原因分析」适合由 Agent 承担

**22 · Agent 间交接：制品传递与门禁**
用任务系统传 handover。交接的是制品不是对话。
每一道交付物都要过门禁，而门禁在 agent 之外。

**23 · 确定性边界：不应交给 Agent 的工作**
收尾。**凡是「必须每次都一样」的事，一律不交给 agent。**
真实案例：自动拆解会过度拆分，一个小修复被拆成五个串行子任务，
五倍开销零收益。以及：不要信 agent 的自我报告，覆盖率要自己跑，
改动要自己从版本库提取。

---

## 七、先发哪五篇

二十三篇是大工程，但学员最急的只有两件事：**手上那个需求做不做得成、怎么开始。**
第 01 至 05 篇解决这两件事，发完他们就能自己判断该做什么、找谁。
其余十七篇慢慢补。

**练习场景 2026-09 由「差旅申请」改为「研发变更流程」**，理由是统一：
风险分级那根主轴能在同一条流水线上把四级全举完（L2 查流程答疑、L3 提单追踪、
L4 自动修复并发布生产），而第 21–23 篇本来就用开发流水线，读到那里时
读者已经在这条线上住了二十篇。原设计要拿三个不相干的场景各举一个，
读者每隔几篇重建一次场景。

练习仍统一落在「提单并追踪审批进度」上——L2 到 L3，学员自己扛得住，跑得通有成就感。
发布到生产那个留在第 04 篇当解剖对象，只拆不做。

两条配套约束：

**知识库内容必须虚构。**形态照 SOMP 的 `docs/bizdocs/` 抄，内容全部自造。
真文档传进演示空间再截图等于公开发表内部资料，而计划书第五节自己写过
「别指望每张图手工涂抹」。三份文件在 `01-demo-kb/`，版本固定。

**21–23 篇不需要再搭一遍。**那三篇是解剖一条已经在跑的流水线，
本节「样例先行」的要求只约束 01–05；那边的样例已经做完了，
就是 `somp-flows-analysis.md` 那一遍通读。

## 八、尚未决定

**技术路线选型：现有二十三篇里没有它的位置（2026-09-06 记，待安置）。**

问答 Agent 底下至少三条路线，风险特征差着量级：

| 路线 | 怎么找答案 | 输出是什么 | 谁在决定答案 | 自检成本 |
|---|---|---|---|---|
| 索引直答 | 索引脚本按规则命中 | 把命中的知识**原样吐出** | 索引规则 | 零，输出就是原文 |
| 检索增强 | 按相似度召回片段 | 模型**基于片段**改写 | 一半规则一半模型 | 核片段有没有走样 |
| 通读原文 | **模型自己决定读哪几份** | 模型读完重述 | 几乎全是模型 | **核到条款** |

**knodo 走第三条**，全系列的控制措施因此都是按第三条设计的——这一点要在某处讲明，
否则读者拿去套别的平台会错配。

选型看六个因素（站主给）：用户对幻觉的容忍度、知识库完整性、要求的知识覆盖率、
问答命中率、知识的类型（文件／图片／音频／视频）、业务本身答错的代价。
**每条路线都有换不掉的代价**，纯索引路线答得准但答得少——第 01 篇那五道题里，
第 3 题要跨文件组合、第 4 题答案是「没有」，纯索引路线对这两道只能返回未命中。

**为什么难安置**：它既不是 02（风险来源），也不完全是 11（知识库设计）；
它是「动手之前先选哪条路」，而且**它决定了 11 篇该怎么组织知识**，所以次序上在 11 之前。
候选处置：并进第 11 篇前半（编号不动，代价是 11 篇偏长、可能要拆上下两次发）；
或独立成篇重排目录（代价是序章那张目录要重发，而公众号那版已发、改不了）。
**2026-09-06 决定先不定，写到第四部分时再定。**

- 发布节奏
- 是否需要配套的动手环境（学员各自的空间？共享一个练习空间？）
- 第 12 篇「什么时候必须写代码」之后，学员的工程搭档从哪来——
  这不是内容问题，是培训能不能落地的问题
