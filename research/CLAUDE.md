# research/

> L2 | 父级: ../CLAUDE.md

写文章用的原始研究资料。**归档性质，只增不改**——每份实验完成即冻结，
后续若要重跑，另开一个目录，不覆盖旧的。旧数据的价值恰恰在于它记录了当时的结论
是怎么得出的，改了就失去了对账能力。

与 `refs/` 的区别：那里是他人作品（已 gitignore，不可发布）；
这里是本项目自己跑出来的数据，可以公开引用，因此入库。

## 成员清单

agent-series/: AI Agent 实操系列（第三轨「实战」）的规划、调研底稿与逐篇执行单，2026-09。
**这个目录有一处刻意的例外**：`SERIES-agent-plan.md` 是活文档，会随系列推进反复修改，
不适用本目录「只增不改」的归档原则；调研快照与执行单适用。
之所以仍放这里，是因为它们互为上下文，拆开会让规划失去依据。

    SERIES-agent-plan.md        二十三篇的规划：定位、读者、写作规范、样例流程、
                                完整目录与每篇产出物。**活文档，会改。**
    knodo-platform-facts.md     Knodo 平台能力与边界事实清单（快照）。
                                通读 255 文件产出，含十七条「原以为 X 实测是 Y」。
                                核心判断：业务顾问能独立搭到「能跑」，搭不到「可信」，
                                卡点在打包注册、验证、约束三处。已脱敏一轮。
                                **它是快照，按本目录规矩不改**；2026-09 实操发现的两处偏差
                                （工作指引未必是 frontmatter+正文两段结构；小知识库下检索
                                根本不触发）记在 `01-build-log.md` 三之一与三之四，
                                **读这份快照时要一并读那里**。
    somp-flows-analysis.md      SOMP 业务流程与可 Agent 化分析（快照）。
                                十二个 agent 环节的输入输出、十三项确定性设施、
                                MVP 依赖顺序（agent 到第 4 步才出场）、十五条反直觉结论。
    01-build-checklist.md       第 01 篇的样例先行执行单。计划书要求「先真搭一遍再写文章」，
                                这份是那一遍的操作指令：三项不可逆决策（演示身份、取景宽度、
                                贯穿场景）、五步搭建按「做什么／怎么做／应看到什么／常见错误」
                                四段展开、七张截图清单与压缩规格、跑完要回答的七个问题。
                                **指令与实录分开落盘**——实测结果另开 `01-build-log.md`，
                                本目录只增不改，混写就失去对账能力。
    01-knodo-operations.md      第 01 篇的**平台操作层**，与上面那份方法层分开：这份讲 knodo
                                怎么点，那份讲为什么这么搭。**分层的理由是保质期不同**——
                                方法换平台照样成立，点击路径 knodo 改一次版就废一半，
                                过期整份替换、方法层一字不动。
                                源自通读官方文档 `ref doc/llms-full.md`（19571 行）与勘误对照，
                                **每条标证据等级、行号可回溯，文档答不了的一律留 ⬜ 空白不猜**——
                                目前十六处，集中成第六节「待登录确认清单」十一项。
                                **两条最重的已定案（2026-09 由勘误与原始取证 JSON 升级为 🟢）**：
                                会话消息的 35 个字段里**没有任何出处类字段**，所以「回答会注明来源」
                                是模型正文行为不是平台能力，且实测它**会编造出处**——
                                第 02 篇「结果可自检」因此改口径为「你能回去对，主语是你不是它」；
                                知识库检索是 **Bot 在沙箱里用 Bash/Read/grep 逐份读文件**，
                                不是分块向量召回（边界：不能反证平台没有别的索引通道）。
                                零之四单列平台的**静默失败形态**六种（状态全绿而功能为零、
                                Agent 自报成功、解析失败与「真的没有」同形…），立的第一原则是
                                「写操作一律以回读目标资源判定，不采信成功提示」——
                                与第 14 篇「别信 agent 自报」是同一条原则的两面。
                                第七节另记七处官方文档自相矛盾（按钮名、字段、Tab 名、
                                单文件上限各两版），提醒下次读文档时哪几处不可信。
    01-build-log.md             第 01 篇的**实测实录**，与 checklist（指令）分开落盘，
                                本目录只增不改，混写就失去对账能力。2026-09 由另一条会话
                                走 REST API 跑通全流程产出（**一次界面都没点过，
                                所以纯界面问题原样留给站主**）。
                                最重的一条在三之一：**那道跨文件两跳题的设计失败了**——
                                它答对了，但工具轨迹显示它把三份文件整篇读进上下文，
                                根本没走检索。结论不是「平台检索强」，是**知识库小到
                                让检索这一环不必存在**，那个准是规模给的不是能力给的。
                                由此给第 11 篇长出「规模拐点」这个本来没有的题目。
                                另记：工作指引不生效由 🟡 升 🟢（botId 传/不传对照实验）、
                                「传完要等索引」被推翻、首字延迟 12–18 秒、
                                消息字段 33→35、`modelId` 全 null。
    01-demo-kb/                 第 01 篇的演示知识库，三份**虚构**研发流程制度。
                                **形态照 SOMP 的 docs/bizdocs/ 抄，内容全部自造**——真文档
                                传进演示空间再截图等于公开发表内部资料，脱敏只能从源头解决。
                                结构是刻意设计的：《管理办法》第三条只说高风险变更谁批、
                                附什么材料，**不说哪些变更算高风险**，那写在《风险分级表》里，
                                于是「改支付回调谁批」必须跨两份文件两跳——**用来逼平台暴露它
                                「只有关键词／文件级检索、没有向量」那条边界**。
                                另埋一个真空：三份都写了高风险须附回滚方案，
                                没有一份写执行回滚由谁批，用来看它是承认不知道还是编。
                                **2026-09 由差旅场景换来**，理由是统一——风险分级 L1–L4
                                能在同一条流水线上把四级全举完，而 21–23 篇本来就用开发流水线。
                                版本固定，02 / 03 / 04 / 05 / 11 / 15 / 16 / 20 / 21–23 沿用。
                                　　研发变更管理办法.md  变更分类、审批原则与权限、变更窗口
                                　　提单与验收流程.md    提单要素、状态流转、时限、验收与退回
                                　　变更风险分级表.md    高／中／低三级的判定条目

    合规输入要求（ISO/IEC 42001）**不在本目录**——它是外部内部文档，标题与正文带公司名，
    留在 drafts/research/ 不入库。规划文件里已提炼出所需结论且已脱敏。


context-experiment/: 上下文对照实验，2026-08-27，60 次，零失败。
起因是要为第 3 篇找证据，假设是「喂给模型的资料越多越坏事」。
**结论是假设被推翻**：A(4.1k token) / B(97.2k，A 的严格超集) / C(9k，A+项目地图)
三组在四道核心判别题上全部 20/20 满分，53 份无关文件的噪声 60 次里 0 次带偏。
真正的差异在别处——地图能传递「约定」（写 L3 头部：55% → 100%，p=0.0012），
传递不了「事实」（改对 feishu.ts 的字段手抄：A 10%、B 90%、C 0%）。

    METHOD.md    方法与三次事故记录。第二条最值钱：打分器 v1 只认字面关键词，
                 把「在 categoryMatch 那块下面加一段」判为未复用解析器，
                 给出的数字完美支持假设。人工复核六个失分样本才发现是仪器坏了。
    RESULTS.md   全部统计结果，含 Wilson 置信区间与 Fisher 精确检验
    wave.json    60 次逐条打分的原始记录
    score.py     打分器 v2，按行为判定而非词汇判定
    dims.py      扩展维度（改动文件数、是否触及第二数据源等），在出总表前定义
    analyze.py   统计分析
    runner.sh    单次运行器，原子写入——先写临时文件、跑完才 mv，
                 因为「文件存在」必须等于「跑完了」，否则会统计到抢跑的样本
    task.md      三组共用的任务描述，不含任何约束提示，约束全埋在源码里
    out/A|B|C/   60 份原始输出，各 20 份

复现：三组上下文由仓库源码现场拼装（见 METHOD.md），因此 `ctx_*.txt` 不入库。

**这批数据的用途：当证据引用，不写成文章。**

曾把「60 次对照实验全过程」列入待写清单，后来删了——它过不了本项目自己的
判断标准：一个做行政的人读完，明天做不了任何不一样的事。对照实验、置信区间、
显著性检验，对本博客的目标读者（零基础普通大众）是纯粹的门槛。

正确用法见第 3 篇那一句：「同一道题跑六十次，只有原文在手的那组发现了那个坑」。
一句话带出结论，不展开方法。读者得到的是「他真去测过」这个可信度，不是一堂统计课。

法则: 成员完整·一行一文件·父级链接·技术词前置

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
