## Node / Vite
node_modules/
**/node_modules/

dist/
**/dist/

## Tooling caches
.cache/
**/.cache/
.vite/
**/.vite/

## Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

## OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

## Environment variables
.env
**/.env
!.env.example

## Agent 运行时状态
# 两处讲究，缺一不可：
# 1) 用 /* 而非目录形式——目录整体被忽略时 git 不递归进入，下方 ! 例外会失效
# 2) 用 **/ 前缀——含斜杠的模式默认只匹配本目录，而 OMC 会在任意子目录留状态
**/.omc/*
!**/.omc/skills/

## 参考资料：他人版权内容，仅供本地风格研习
# 切勿放回 docs/ —— 那里是 GitHub Pages 发布根，入库即等于公开转载
refs/

## 博客草稿（正式内容的真相在飞书，草稿不入库）
drafts/

## 人物视角 skill：基于他人作品蒸馏，仅供本地写作参考
# 产出本身是原创提炼（不含原文），但对象是未授权的在世作者，
# 故不公开分发。只挡这一个，不封整个 .claude/——项目级配置仍应入库。
.claude/skills/xifeng-perspective/

## 公众号转换产物
# 纯中间物，由 scripts/wechat/build.mjs 随时重建，与 docs/ 同理：
# 能被重建的东西入库只会制造「产物与源码不同步」的假象
scripts/wechat/out/

## 正文图片的上传去重缓存
# 键是图片内容 hash，值是素材库回的 media_id 与 mmbiz 地址。
# 与 out/ 同理：删了重跑就有，代价只是多传一次。
# 更要紧的是它是**本机与本账号**的对应关系——素材库是账号私有的，
# 入库等于把另一个账号的 media_id 发给协作者，那些 id 在他那儿一个都不存在。
scripts/wechat/.image-cache.json
.gstack/
